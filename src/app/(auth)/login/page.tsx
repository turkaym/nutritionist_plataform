import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function LoginPage() {
    return (
        <PagePlaceholder
            title="Login"
            description="Authentication page placeholder for customer and admin sign-in flow."
        />
    );
}