import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function MyAccountPage() {
    return (
        <PagePlaceholder
            title="Mi cuenta"
            description="Authenticated account area placeholder for future customer profile and account settings."
        />
    );
}